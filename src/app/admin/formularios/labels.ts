/** Small label map local to the forms domain (not shared via `_lib/status.ts`). */
export const FORM_CATEGORY_LABEL: Record<string, string> = {
  inicio: "Inicio",
  durante: "Durante",
  cierre: "Cierre",
  custom: "Personalizado",
};
